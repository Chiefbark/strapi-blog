{
  "routes": [
    {
      "method": "GET",
      "path": "/posts",
      "handler": "posts.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/posts/count",
      "handler": "posts.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/posts/:slug",
      "handler": "posts.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}
